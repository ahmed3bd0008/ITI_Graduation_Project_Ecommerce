@model List<MahaleSystem.Models.Manahel>
@foreach (var item in Model)
{
    <li class="nav-sub-item">
        <a class="nav-sub-link" asp-action="Details" asp-controller="Manahels" asp-route-id="@item.Id">@item.NickName</a>
    </li>
}
